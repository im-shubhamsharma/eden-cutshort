import styled from "styled-components";

export const WelcomeContainer = styled.div`
  /* border : 2px solid ${({theme}) => theme.textPrimaryColor}; */

  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  gap: 0.5rem;
  
`